$ sort -n numbers.txt | head -1 // сортираме числата в числов ред и взимаме първия ред (по-малкото)
$ sort -n -r numbers.txt | head -1 // сортираме числата в числов и обратен ред и взимаме първия ред (по-голямото)
